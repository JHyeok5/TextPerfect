// 컴포넌트별 기본 클래스명
export const CLASSNAMES = {
  button: 'rounded font-semibold transition-colors duration-200 flex items-center justify-center gap-2',
  card: 'rounded-lg bg-white shadow-soft',
  modal: 'bg-white rounded-lg shadow-lg',
  badge: 'inline-flex items-center px-2 py-0.5 rounded text-xs font-bold',
  input: 'border rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-primary',
};

// 애니메이션 duration
export const ANIMATION_DURATION = {
  fast: 150,
  normal: 400,
  slow: 700,
};

// 브레이크포인트
export const BREAKPOINTS = {
  xs: 400,
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  '2xl': 1536,
}; 